important points:

1. balance the data
2. standard scaling performs better than minmax scaling with SVM, and improves its results to those from random forest classifier
3. important to choose a smaller time window for PAA, but not too small to increase the computation
